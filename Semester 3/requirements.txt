# Python 3.7 with TensorFlow 1.15 compatible versions
# Install with: pip install -r requirements.txt

# Core TensorFlow (must be 1.15 for this codebase)
tensorflow==1.15.0

# Core data analysis (compatible with Python 3.7 and TF 1.15)
numpy>=1.16.0,<1.19.0  # NumPy 1.19+ drops Python 3.5 support, but 1.18.x works well with TF 1.15
pandas>=0.25.0,<1.2.0  # Pandas 1.2+ requires Python 3.7.1+, 1.1.x is stable for Python 3.7.0
scipy>=1.3.0,<1.6.0    # SciPy 1.6+ requires Python 3.7+, 1.5.x is compatible

# Visualization
matplotlib>=3.1.0,<3.3.0  # Matplotlib 3.3+ has some compatibility issues with older setups
seaborn>=0.10.0,<0.12.0   # Seaborn 0.11.x works well with these versions

# Additional dependencies used in the code
argparse  # Usually included in Python stdlib, but listed for completeness

# Optional but recommended for better performance
h5py>=2.10.0,<3.0.0  # For saving/loading Keras models (if needed)
protobuf>=3.8.0,<3.20.0  # Required by TensorFlow, version constraint for compatibility

# For reproducibility and stability
six>=1.12.0
absl-py>=0.7.0

# Development tools (optional)
# jupyter>=1.0.0
# ipython>=7.8.0,<8.0.0  # IPython 8+ requires Python 3.8+

# Note: Installation order matters for some packages
# If you encounter issues, try:
# 1. pip install numpy==1.18.5
# 2. pip install tensorflow==1.15.0
# 3. pip install -r requirements.txt